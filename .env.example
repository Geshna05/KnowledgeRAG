# No API key needed - this uses Ollama running locally (100% free).
# Only change these if you renamed your model or run Ollama on a different host/port.
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=llama3.1

# Optional overrides (defaults shown)
CHROMA_DIR=./chroma_data
DB_PATH=./study_assistant.db
